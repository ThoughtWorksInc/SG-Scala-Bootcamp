val sequences = Seq(Seq(1,2,3), Seq(4,5), Seq(6,7,8))

//uncomment below code and find what would be output1 and output2

//val output1 = for {
//  Seq(a,b,c) <- sequences
//} yield a + b + c

//val output2 = sequences.map {case Seq(a,b,c) => a + b + c}
